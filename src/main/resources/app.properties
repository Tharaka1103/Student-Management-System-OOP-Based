# Application Configuration
app.name=Class Management System
app.version=1.0

# Pagination
pagination.pageSize=10

# Session Timeout (in minutes)
session.timeout=30

# Password Encryption
password.encryption.algorithm=SHA-256